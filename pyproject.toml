[project]
name = "crm-signals"
version = "0.1.0"
description = "Scrape prospect sites and push signals to Notion"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
